{
  "name": "@fmented/idb-serializer",
  "version": "0.0.1",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/fmented/idb-serializer"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "patch": "pnpm build && pnpm version patch && git push && pnpm publish --access=public",
    "minor": "pnpm build && pnpm version minor && git push && pnpm publish --access=public",
    "major": "pnpm build && pnpm version major && git push && pnpm publish --access=public"
  },
  "devDependencies": {
    "fake-indexeddb": "^5.0.2",
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vite-plugin-dts": "^3.9.1",
    "vitest": "^1.6.0"
  }
}
